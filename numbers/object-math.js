let result;

// PI
result = Math.PI;

// redondear
result = Math.round(2.3);
result = Math.round(2.7);
// redondear hacia arriba
result = Math.ceil(2.1);
// redondear hacia abajo
result = Math.floor(2.9);

// raiz cuadrada
result = Math.sqrt(144);

// valor absoluto
result = Math.abs(-500);

//potencia
result = Math.pow(2, 4);

// minimo
result = Math.min(3, 4 ,2, -3);
//maximo
result = Math.max(9, 4 ,2, -3);

// aleatorio 
result = Math.random();
// aleatorio dentro de un rango
result = Math.floor(Math.random() * 30);


console.log(result);
